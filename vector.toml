[sources.app_logs]
type = "docker_logs"
docker_host = "unix:///var/run/docker.sock"

[sinks.clickhouse]
type = "clickhouse"
inputs = [ "app_logs" ]
database = "logs"
endpoint = "http://172.17.0.3:8123"
table = "logs"
compression = "gzip"
skip_unknown_fields = true
